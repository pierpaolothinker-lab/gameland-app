import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Tressette4IncPage } from './tressette4-inc.page';

describe('Tressette4IncPage', () => {
  let component: Tressette4IncPage;
  let fixture: ComponentFixture<Tressette4IncPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Tressette4IncPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
